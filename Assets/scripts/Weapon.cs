using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Weapon : MonoBehaviour
{
    public Guns gun;
    // Start is called before the first frame update
    void Start()
    {
        //Instantiate(gun.gun, new Vector3(-0.4389846f, 4.438357f, 13.35186f), Quaternion.identity);
        //transform.ra
       // GameObject.Instantiate(gun.gun, transform.position , transform.rotation * Quaternion.Euler(180f, 0f, 0f));

    }

    // Update is called once per frame

}