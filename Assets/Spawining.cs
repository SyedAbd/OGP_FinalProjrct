using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Spawining : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        transform.position = new Vector3(-0.4389846f, 4.438357f, 13.35186f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
